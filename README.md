#include <iostream>
using namespace std;

int main() {
	setlocale(0, "");

	cout << "Выберите время года: ";

	short g;
	cin >> g;
	if (g == 1) {
		cout << "Сейчас лето, следует одеться легче и нанести солнцезащитный крем.";
	}
	else if (g == 2) {
		cout << "Сейчас осень, следует одеться тепло и взять с собой зонт.";
	}
	else if (g == 3) {
		cout << "Сейчас зима, следует одеться ОЧЕНЬ тепло.";
	}
	else if (g == 4) {
		cout << "Сейчас весна, следует одеться тепло и взять с собой зонт.";
	}
	else {
		cout << "Времён года всего 4";
	}
	int _;
	cin >> _;
}
